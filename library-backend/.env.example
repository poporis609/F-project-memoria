# Backend Environment Variables Example

# 서버 설정
DEBUG=true
HOST=0.0.0.0
PORT=8000

# 데이터베이스 설정
DATABASE_URL=postgresql+asyncpg://username:password@host:port/database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=your_database
DB_USER=your_username
DB_PASSWORD=your_password

# AWS S3 설정
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=your_access_key_id
AWS_SECRET_ACCESS_KEY=your_secret_access_key
S3_BUCKET_NAME=your_bucket_name
S3_REGION=ap-northeast-2

# AWS Step Functions 설정 (동영상 프리뷰 생성용)
VIDEO_PREVIEW_STATE_MACHINE_ARN=arn:aws:states:ap-northeast-2:YOUR_ACCOUNT_ID:stateMachine:Preview_thumbnail_library

# JWT 설정
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# CORS 설정
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# 프로젝트 정보
PROJECT_NAME=Library Management API
VERSION=1.0.0
DESCRIPTION=FastAPI backend for library management system